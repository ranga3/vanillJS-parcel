const Header = () => {
    const template = `
    <header>
    <h1> Parcel App </h1>
    <p> Boilerplate for a simple JS workflow using parcel </p>

    </header>
    `;
    return template;
}
export default Header;